# agent-jail-linux-x64

The linux-x64 binary for [agent-jail](https://www.npmjs.com/package/agent-jail).

**Do not install this package directly.** Install `agent-jail` instead;
npm will pick the right platform package automatically via
`optionalDependencies`.
